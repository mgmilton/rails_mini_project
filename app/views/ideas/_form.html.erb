<%= form_for [@user, @idea] do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.label :category_id %>
  <%= f.collection_select(:category_id, Category.order(:name), :id, :name) %>

  <%= f.label :image %>
  <%= f.collection_select(:image_ids, Image.all, :id, :url, {}, {:multiple =>true})  %>

  <%= f.submit %>
<% end %>
